# Dependencies for the load-test *driver* (loadtest/smoke_load.py) only.
#
# The driver is a separate process from the agent under test: it hammers the
# running uvicorn server (httpx) and samples that server process's CPU% + RSS
# from the outside (psutil). It does NOT import the copilot package, so it does
# not need the agent's dependencies — install these into a dedicated
# loadtest/.venv (see run.sh) and boot the agent itself with agent/.venv.
#
#   python -m venv loadtest/.venv
#   loadtest/.venv/bin/pip install -r loadtest/requirements.txt
httpx>=0.27
psutil>=5.9
