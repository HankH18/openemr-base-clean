meta {
  name: Get Conversation
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/conversations/{{conversation_id}}
  body: none
  auth: none
}

docs {
  GET /v1/conversations/{id} — read a conversation's turns back in order
  (oldest first). Returns { "messages": [ {role, content}, ... ] }.
}
